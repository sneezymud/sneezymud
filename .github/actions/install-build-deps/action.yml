name: Install build dependencies
description: Install apt packages required for building SneezyMUD
inputs:
  extra-packages:
    description: Additional apt packages to install
    required: false
    default: ""
runs:
  using: composite
  steps:
    - name: Install dependencies
      shell: bash
      env:
        EXTRA_PACKAGES: ${{ inputs.extra-packages }}
      run: |
        set -e
        sudo apt-get update
        export DEBIAN_FRONTEND=noninteractive
        packages="build-essential ca-certificates ccache clang cmake git libclang-rt-dev libboost-atomic1.83-dev libboost-filesystem1.83-dev libboost-program-options1.83-dev libboost-regex1.83-dev libboost-system1.83-dev libcurl4-openssl-dev libmariadb-dev lld llvm ninja-build pkgconf"
        if [ -n "${EXTRA_PACKAGES}" ]; then
          packages="${packages} ${EXTRA_PACKAGES}"
        fi
        TZ=utc sudo apt-get install --yes --no-install-recommends ${packages}
