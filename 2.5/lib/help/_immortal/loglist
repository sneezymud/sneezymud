LOGLIST

This command will display the available logs.  Each log represents a given
game session, and a log is terminated at reboot.  The current log is in
logcurrent.

To refer to a log for log related commands, you only need to use part of the
name...

See Also: DEATHCHECK
